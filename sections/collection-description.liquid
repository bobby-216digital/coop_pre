{% if collection.description != blank %}
<div class="collection-description text-center py-20 lg:pb-40" {% if section.settings.bg_color != blank %}style="background-color: {{ section.settings.bg_color }};"{% endif %}>
  <div class="h3 collection-description__content px-20 md:px-40">{{ collection.description }}</div>
</div>
{% endif %}

{% schema %}
    {
      "name": "Collection Description",
      "settings": [
        {
          "id": "bg_color",
          "type": "color",
          "label": "Background Color"
        }
      ]
    }
  {% endschema %}